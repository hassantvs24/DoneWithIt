import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
    text: {
        color: 'blue',
        fontSize: 20
    },

    heading: {
        fontSize: 30,
        fontWeight: 'bold',
        color: 'black',
    }
});

export default styles;